[package]
name = "perf-lab"
version = "0.1.0"
edition = "2021"

# std-only on purpose: no deps => reproducible, auditable benches.

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"

[dependencies]
parking_lot = "0.12"
